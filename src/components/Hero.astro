<div class="hero">
  <div class="hero-text">
    <h1 class="hero-intro">
      Hi, I'm <span class="hero-highlight"
        >Tom<span class="hero-type">_.</span>
      </span>
    </h1>
    <span class="hero-subtext"
      >I'm a student, developer, but most importantly a <span
        class="hero-underlined">tinkerer</span
      >.</span
    >
  </div>
  <button class="contact-button">// contact me</button>
</div>

<style lang="scss">
  .hero {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: flex-start;
    gap: 2.5rem;
    font-family: "Fira Sans", sans-serif;
    font-size: 2.25rem;
    line-height: 2.5rem;
    font-weight: 700;
    color: var(--color-text);
    padding: 6rem 2.5rem;

    .hero-text {
      display: flex;
      flex-direction: column;
      gap: 0.75rem;
    }
    .hero-highlight {
      display: inline-flex;
      font-family: "Fira Code", monospace;
      color: var(--color-accent);
    }

    .hero-type {
      overflow: hidden;
      white-space: nowrap;
      animation: type 0.75s steps(4, end) infinite alternate;
      color: var(--color-text);

      &:before {
        content: " ";
        display: inline-block;
      }
    }

    .hero-subtext {
      font-size: 1.875rem;
      line-height: 2.25rem;
      font-weight: 400;
      color: var(--color-text);
      margin: 1rem 0;

      .hero-underlined {
        display: inline-block;
        position: relative;
        &::after {
          content: "";
          display: block;
          position: absolute;
          top: 1.8rem;
          width: 100%;
          background-image: url(/underline.svg);
          background-size: contain;
          background-repeat: no-repeat;
          height: 1rem;
        }
      }
    }

    .contact-button {
      font-family: "Fira Code", monospace;
      font-size: 1rem;
      line-height: 1.5rem;
      font-weight: 400;
      padding: 0.75rem 1rem;
      position: relative;

      &::before,
      &::after {
        content: "";
        position: absolute;
        width: 1.5rem;
        height: 1.5rem;
        transition: width 0.25s ease-in-out, height 0.25s ease-in-out;
      }

      &::before {
        top: 0;
        left: 0;
        border-top: 2px solid var(--color-accent);
        border-left: 2px solid var(--color-accent);
      }

      &::after {
        right: 0;
        bottom: 0;
        border-bottom: 2px solid var(--color-accent);
        border-right: 2px solid var(--color-accent);
      }

      &:hover::before,
      &:hover::after {
        width: 100%;
        height: 100%;
      }
    }
  }

  @keyframes type {
    from {
      width: 0;
    }
    to {
      width: 100%;
    }
  }

  @media screen and (min-width: 768px) {
    .hero {
      font-size: 4.5rem;
      line-height: 1;
      justify-content: start;
      padding: 10rem 5rem;
      gap: 5rem;

      .hero-text {
        gap: 1.5rem;
      }

      .hero-subtext {
        font-size: 2.25rem;
        line-height: 2.5rem;

        .hero-underlined {
          &::after {
            top: 2.5rem;
            width: 10rem;
            height: 1.25rem;
          }
        }
      }

      .contact-button {
        font-size: 1.5rem;
        line-height: 2rem;
        padding: 1.25rem 1.75rem;

        &::before,
        &::after {
          width: 2rem;
          height: 2rem;
        }
      }
    }
  }
</style>
